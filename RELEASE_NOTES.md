# 版本说明 v3.0.0-beta

日期：2026.02.12

## 概述
本次为结构强化版本，补齐“副本管理/归档/排序”“系统预设日常生活”“多语言与时间格式统一”等基础设施，并将副本管理入口迁移至计时页以匹配核心流程。

## 新增与调整
1. 副本管理与归档
新增“已存档副本”页面，支持主线/日常分区展示与取消存档。
副本页支持存档操作，存档按钮统一为蓝紫色。
副本页新增“编辑/完成”排序模式，仅在编辑状态可拖拽排序。
存档副本页移除排序能力，保持轻量查看。

2. 日常生活预设
新增系统级日常生活预设（卫生间/睡觉/通勤/吃饭/做饭/休息/游戏）。
系统预设不可删除、不可修改，编辑页提示锁定状态。

3. 计时页入口调整
副本管理入口从设置页迁移至计时页右上角，更贴合记录流程。

4. 多语言与时间格式
中英文双语支持，界面字符串统一走本地化键。
时间显示格式统一为中文样式（yyyy年MM月dd日 HH:mm:ss / HH:mm），避免长度跳变。

5. 排序体验补强
属性页与副本页在切换 Tab/离开页面时自动退出编辑模式，避免误留排序态。

## 已知限制
调试注入数据仅用于开发期，后续将隐藏入口或转为彩蛋。

---

# 版本说明 v2.0.1-beta

日期：2026.02.09

## 概述
补充“日常生活”记录逻辑：计时页可选择日常生活结算；记录编辑与统计不再显示评分与属性绑定项。

## 修复与改进
1. 日常生活记录
计时页支持选择日常生活条目进行记录。
结算与编辑时不显示评分、奖励倍数与属性绑定。
日常生活记录不会参与属性结算。

---

# 版本说明 v2.0.0-beta

日期：2026.02.09

## 概述
本次为重大结构升级，新增“计时页”作为核心入口，记录仅通过计时结算生成；副本页回归模板管理并新增“日常生活”条目；统计页与记录编辑的交互完善；输入校正从“输入时夹紧”改为“失焦后夹紧”，减少干扰。

## 新增与调整
1. 计时页
新增常驻计时页，展示当日剩余时间进度、当前任务持续时间、起止时间。
通过“胜利”结算生成记录，支持评分、奖励倍数与可选信息（地点/备注）。
副本选择为单选，结算后自动清空，进入下一次记录流程。
时间显示统一为中文格式：MM月dd日 HH:mm:ss。

2. 副本管理
副本页仅保留模板管理与编辑入口，移除记录列表与支线记录入口。
新增“日常生活”类别，仅记录名称并显示在副本页专属区块。
日常生活条目支持新增、编辑与删除。

3. 统计与记录编辑
统计页记录列表显示“时长”而非具体时间，长于 24 小时按“X.X日”显示。
记录编辑中，开始/结束时间分离展示，开始时间不可修改，结束时间仅最新记录可调整。
未达预期时隐藏主观评分与奖励倍数，避免重复配置。

4. 输入体验
数字输入统一改为键盘输入并在失焦时夹紧（FocusState 触发）。
未达预期 bonus 支持独立设置（0.1–2.0）。

## 已知限制
计时页暂不支持后台常驻，仅在进入页面时刷新与计时。
统计页记录暂未提供筛选或分组功能。

## 后续建议
1. 主线副本“变体”
为主线副本模板增加“变体”（如：上课/自学/复习），用于在不增加模板数量的前提下区分不同活动的结算规则（倍率、是否按时长、奖励倍数或对特定属性的倍率覆盖）。记录时若模板存在变体，则先弹出变体选择再进入记录页，以降低认知负担并提升快速记录效率。

---

# 版本说明 v1.1.1-beta

日期：2026.02.08

## 概述
本次为小版本补丁，集中修复统计页热力图的“默认定位到最新”问题，并完善热力图的时间标注与布局细节。滚轮输入样式继续微调以提高可读性与紧凑感。

## 修复与改进
1. 统计页热力图
修复“进入统计页时未显示最新”的问题，确保在“最新靠右”时稳定定位到最右侧。
为保证定位稳定性，热力图滚动改为由 UIKit 的 UIScrollView 驱动并直接控制 contentOffset。
新增月份标记与星期标记（周一至周日，当前显示一三五日以减少视觉拥挤）。
优化月份与格子间距，上下留白更均衡。
修复“靠左模式下月份标记偏移到月末”的问题。

2. 滚轮输入体验
滚轮高度调整为更紧凑但可读的 36。
滚轮字号改为 14，并恢复正常缩放比例，避免字符裁切。

## 已知限制
统计页记录暂未提供筛选与分组功能。

## 后续建议
1. 主线副本“变体”
为主线副本模板增加“变体”（如：上课/自学/复习），用于在不增加模板数量的前提下区分不同活动的结算规则（倍率、是否按时长、奖励倍数或对特定属性的倍率覆盖）。记录时若模板存在变体，则先弹出变体选择再进入记录页，以降低认知负担并提升快速记录效率。

---

# 版本说明 v1.1.0-beta

日期：2026.02.05

## 概述
本次版本聚焦“统计页可视化 + 记录可读性”，补齐编辑能力与细节交互。统计页新增热力图与范围联动高亮，并在范围下方展示详细记录与总获取经验；副本相关数字输入统一为滚轮选择以减少视觉跳动；设置页加入外观选项并修复进入后难以退出的问题。

## 新增功能
1. 统计页热力图
常驻显示历史热力图，支持“最新靠右/靠左”排列。
支持按当前时间范围高亮对应日期，采用“记号笔式”连续竖向高亮。
未来日期自动隐藏格子与高亮，保证列结构稳定。
热力图在“最新靠右”时自动滚动到最右侧。

2. 统计页详细记录
新增“记录”列表，显示当前时间范围内所有副本记录。
新增“总获取经验”汇总行，便于快速了解区间收益。
新增“紧凑布局”设置项，用于压缩记录行距。

3. 编辑能力补齐
属性支持编辑（分类名、条目名称、从属、基础值、权重、类型、衰减）。
副本记录支持编辑，自动重算属性收益避免重复累加。

4. 输入与格式体验
副本相关数字输入统一为滚轮选择（时长、评分、倍率、奖励倍数）。
副本记录列表时间显示统一为中文格式：yyyy年MM月dd日 HH:mm。
日期选择器改为中文本地化展示。

## 交互与界面优化
统计页与设置页统一为列表风格，整体视觉与属性页一致。
设置页以弹层方式打开，避免切换 Tab 后停留在设置页。
统计页范围显示改为居中展示，并支持前后切换。
滚轮高度与字号多轮调校，尽量在紧凑与可读性间平衡。

## 已知限制
统计页记录暂未提供筛选与分组功能。
热力图尚未提供月/年刻度或数值提示。

---

# 版本说明 v1.0.0-beta

日期：2026.02.03

## 概述
这是“个人成长追踪软件”的首个可用版本，完成了属性体系与副本记录的闭环，并实现了基础的数值结算与持久化。当前版本以稳定可用为优先，功能保持精简但结构清晰，便于后续持续演进。

## 核心功能
1. 属性体系
支持“分类 + 条目”的两级展示结构。
条目可配置基础值、权重与类型（累积型/衰退型）。
分类分数使用条目经验的加权平均。
条目以等级显示，等级规则为每 100 经验升 1 级。

2. 副本体系
支持“主线副本模板”与“支线记录”。
主线副本可选择按时间计价，支线记录默认固定倍率。
每条记录包含必填信息：名称、关联属性、时间、评分、是否未达预期。
可选信息包含地点、备注、奖励倍数。

3. 数值结算
采用可解释的线性规则，评分转为 0–100% 的 bonus。
未达预期等价于 bonus = 0。
结算会记录每条属性的基础值快照与实际收益，便于未来统计分析。

4. 数据持久化
使用 SwiftData 保存属性、分类、副本模板与记录。
应用重启后数据保留。

5. 界面与交互
主界面为三个 Tab：副本、属性、统计。
属性页支持新增分类与条目、下滑删除、等级展示。
副本页支持模板创建、支线记录创建、记录列表查看。
设置页提供版本与日期信息。

## 关键业务规则
经验获得计算公式：

```
经验 = 基础值 × 副本倍率 × 时间因子 × bonus × 奖励倍数
bonus = 评分 / 100
时间因子 = 分钟 / 60（仅按时间计价时启用）
```

衰退规则：
若条目为衰退型，并且 3 天内无增长，则从第 4 天起按“每日衰减值”递减。

## 数据模型摘要
AttributeGroup：分类，包含名称与条目列表。
RPGAttribute：条目，包含基础值、权重、经验、类型、衰减参数与上次增长时间。
DungeonTemplate：主线副本模板，包含名称、是否按时间计价与影响属性列表。
DungeonEffectTemplate：模板与属性的倍率映射。
DungeonLog：副本记录，包含评分、时长、奖励倍数与是否未达预期。
DungeonEffectLog：记录中的收益快照与实际收益。

## 已知限制
统计页暂为空白，仅保留入口。
暂不支持编辑已创建的属性/副本模板（仅支持新增与删除）。
衰退不会在后台定时执行，仅在读取时计算展示。
删除操作不可撤销。
仅本地单机数据，暂不支持同步。

## 后续建议
为属性条目提供编辑入口（基础值、权重、类型）。
统计页增加趋势图、近 7 天游玩/学习收益汇总。
为副本记录增加批量快速录入或模板快捷入口。
增加“属性详情页”展示历史记录与成长曲线。
